[project]
name = "symbolic-options"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "craftax>=1.4.5",
    "flax>=0.10.4",
    "gymnax>=0.0.8",
    "hydra-core>=1.3.2",
    "jaxtari",
    "omegaconf>=2.3.0",
    "optax>=0.2.4",
    "rtpt>=0.0.4",
    "wandb[media]>=0.19.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
jaxtari = { path = "JAXAtari", editable = true }
